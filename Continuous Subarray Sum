class Solution:
    def checkSubarraySum(self, nums, k):
        mod_map = {0: -1}
        total = 0

        for i, num in enumerate(nums):
            total += num
            mod = total % k

            if mod in mod_map:
                if i - mod_map[mod] >= 2:
                    return True
            else:
                mod_map[mod] = i

        return False
